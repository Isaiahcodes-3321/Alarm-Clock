import 'package:flutter/material.dart';

class ViewAlarm extends StatelessWidget {
  const ViewAlarm({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Center(child: Text('Alarm'),),
    );
  }
}
import 'package:flutter/material.dart';

class ViewStopWatch extends StatelessWidget {
  const ViewStopWatch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      child: Center(child: Text('Stop Watch'),),
    );
  }
}